version: "3"
services:
  integracao_reportit_php:
    container_name: integracao_reportit_php
    build:
      context: "./.docker"
      dockerfile: Dockerfile
    ports:
      - "9002:9000" # fpm
    volumes:
      - "./.:/var/www/html"

 
